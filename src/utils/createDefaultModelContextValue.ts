import { model_context_value } from '../types';

export function createDefaultModelContextValue(): model_context_value {
    return {
        model: {},
    };
}

export default createDefaultModelContextValue;
